//  参数配置表
let ipDeault = 'localhost'; //IP
let urlAddress = ':8080/UsedCar/TestServlet'; // 地址

const httpConfig = {
  //默认的URLIp路径
  ipDeault: ipDeault,
  //默认的URL地址路径
  urlAddress: urlAddress,
}
export {
  httpConfig
}